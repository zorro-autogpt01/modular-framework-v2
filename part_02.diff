diff --git a/modular-framework/modules/llm-tester/src/llm.js b/modular-framework/modules/llm-tester/src/llm.js
index 0a01400..4b8ccc7 100644
--- a/modular-framework/modules/llm-tester/src/llm.js
+++ b/modular-framework/modules/llm-tester/src/llm.js
@@ -1,5 +1,5 @@
 import fetch from "node-fetch";
-import { logError, logDebug } from "./logger.js";
+import { logError, logDebug, logInfo } from "./logger.js";
 
 function joinBase(base, path) {
 const b = base.endsWith("/") ? base.slice(0, -1) : base;
 return `${b}${path}`;
 }
@@ -58,6 +58,10 @@ export async function chatCompletion({ baseUrl, headers = {}, model, messages =
 };
 
 logDebug("LLM Gateway request", { url, model, messageCount: Array.isArray(messages) ? messages.length : 0 }, "llm");
+  // Full payload logging for Splunk visibility
+  try {
+    logInfo("LLM Gateway chat payload", { url, model, messages }, "llm");
+  } catch {}
 
 let response;
 try {
@@ -102,6 +106,10 @@ export async function chatCompletion({ baseUrl, headers = {}, model, messages =
 }
 
 const content = extractContent(json);
 logDebug("LLM Gateway success", { contentLength: content?.length ?? 0 }, "llm");
+  // Full completion content logging for Splunk visibility
+  try {
+    logInfo("LLM Gateway chat completion", { model, content }, "llm");
+  } catch {}
 
 return { raw: json, content };
 }
