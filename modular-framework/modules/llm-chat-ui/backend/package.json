{
  "name": "llm-chat-ui-backend",
  "version": "1.0.0",
  "description": "Advanced LLM chat interface backend with intelligent features",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:init": "node -e 'require(\"./db\").initDb().then(() => { console.log(\"DB initialized\"); process.exit(0); })'",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.11.3",
    "axios": "^1.6.2",
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
